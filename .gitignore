node_modules/
dist/
build/
.env
*.log

# electron-vite / electron-builder 輸出
out/
release/
.vite/
*.tsbuildinfo
# IRMS_App/build/ 是 electron-builder 的 buildResources 來源目錄(圖示等),
# 不是編譯產出物,必須進版控——排除頂層 build/ 規則對它的例外
!IRMS_App/build/
!IRMS_App/build/**
# electron-vite dev 執行期會把解析後的設定寫成帶時間戳的暫存檔,不該進版控
electron.vite.config.*.mjs

# SQLite 執行期資料庫(改存於 userData,不進版控)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Obsidian:個人工作區狀態不進版控,共用設定(外掛/外觀/模板)保留
.obsidian/workspace.json
.obsidian/graph.json

# Claude Code 的本機權限設定與它自動留下的 .bak;逐台機器不同,不該進版控
# (.claude/CLAUDE.md 是專案層級規則,要進版控,故只排除 settings.local.json*)
.claude/settings.local.json*
