{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "Capability for the boot-splash window — only needs to receive the two handoff signals (splash-advance/splash-fade-out-frame), no DB/BLE/window-control access.",
  "windows": ["splash"],
  "permissions": [
    "core:event:default"
  ]
}
