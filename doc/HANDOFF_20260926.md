# Handoff 2026-09-26 (for GPT 6 Astra / next agent)

Branch `claude/irms-ui-v3` (draft PR #12, stacked on PR #9). Released: app v1.2.0-beta.14, firmware v1.0.1-beta.1 (beta channel). Context: `doc/coding log/log_20260925_realdevice_fixes_autopush_ui_v3.md`.

Verified: module loading in a real build (telemetry 01:13:56Z `[module:session-tips] activated`).

Remaining — all need the device + a person; judge each from telemetry:
1. First automatic OTA: connect beta.14 app while idle → expect `ota_progress` events, device reboot, reconnect, device reports 1.0.1-beta.1.
2. Over-limit alarm → mute (`CMD:ALARM_OFF`) → re-arm after 30 s (`CMD:ALARM_ON` again).
3. ESP32 power cut mid-session → `connection {connected:false}` then auto-reconnect, alarm re-arms.
4. Force-kill app mid-session → next run shows the session `abandoned` (History badge 未正常結束).
Record results on issue #3.

Watching telemetry: `set -a; . "$USERPROFILE/.irms-telemetry.env"; set +a; node tools/telemetry/watch.js`
(prints non-packet events and ERR/angle transitions for the newest run; never echo the token).
