# AI Coding Log - 2026-06-11 01:38:43

## 目的 (Objective)
解決自訂動作（例如 `test_1`）在特定動作（如直膝後擺 `segment_extension`）下無法正確觸發 Reps 與提示音/LED 的問題。

## 動作記錄 (Action Log)
1. **分析感測器讀數與觸發條件**：
   - 經分析，直膝抬腿 (`segment_elevation`) 與直膝後擺 (`segment_extension`) 需要滿足「膝關節打直」的條件（原判定為 `correctedKnee <= toleranceVal`，即 `≤ 10°`）。
   - 若使用者設定的動作容錯值較嚴格（如 5° 或 10°），且感測器存在輕微偏置或抖動，導致膝關節角度大於容錯度（例如 11° 或 12°），觸發將會失效。
2. **放寬直膝判定容錯度 (`app.js`)**：
   - 在 `checkActionCondition` 與 `calculateSessionMetrics` 中，將膝關節打直的判定門檻放寬至 `Math.max(15, toleranceVal)`，確保在設定嚴格容錯度時，膝關節仍有至少 15° 的合理打直容許空間，避免因細微抖動無法觸發。
3. **新增即時診斷輸出 (`app.js`)**：
   - 於 `handleAngleNotification` 接收 BLE 角度包時，新增每 3 秒一次（每 30 個數據包）的 `[DIAG]` 狀態診斷日誌，輸出至側邊欄的 `STATUS LOG` 面板：
     - 當前啟動的動作 ID、名稱、動作類型、目標角度及容錯值。
     - 即時的三Segment corrected角度值（大腿、小腿、膝夾角）。
     - 各條件判定的布林值結果（`inZone`, `mustReturnToRest`, `isAtRest`）。
   - 此診斷輸出能幫助使用者在運動時立刻看見「角度是否已經達到設定範圍」或「正負號（Invert）是否相反」。
4. **確認韌體修復 (`IRMS_Sensor.ino`)**：
   - 確認使用者已成功在韌體端修復 MPU6050 陀螺儀 Gyro Pitch/Roll 角速度變數對調的 Bug，並將雙短響與雙閃的阻塞式 delay 優化為非阻塞式的非同步 `Task_LED` 結構。
5. **語法檢查**：
   - 使用 `node --check` 驗證 `public/app.js` 的語法，結果完全正確。
