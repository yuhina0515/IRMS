# AI Coding Log - 2026-06-11 01:24:43

## 目的 (Objective)
將 Designated Action (指定動作) 中的寫死項目移除，並新增自訂動作管理頁面，使使用者能自己新增/編輯動作，或透過「記憶姿勢」來直接擷取當前感測器讀數作為目標值。

## 動作記錄 (Action Log)
1. **資料庫結構升級 (`db.js`)**：
   - 新增 `custom_actions` 資料表，欄位包含 `id`, `name`, `protocol`, `targetAngle`, `tolerance`, `holdTimeMs`, `triggerType`。
2. **後端 API 實作 (`server.js`)**：
   - 新增針對自訂動作的 CRUD 路由：
     - `GET /api/custom_actions`
     - `POST /api/custom_actions`
     - `PUT /api/custom_actions/:id`
     - `DELETE /api/custom_actions/:id`
3. **前端介面變更 (`index.html` & `style.css`)**：
   - 側邊欄新增 **Actions** 導覽連結與 Pulse 圖示。
   - 新增 exercise / action 編輯管理頁面，顯示卡片清單、支援編輯、刪除與還原預設範本。
   - Dashboard 側邊設定面板新增「記憶姿勢 (Record Current Pose)」按鈕。
   - 新增「新增/編輯動作」與「記憶姿勢」兩大 Modal 彈窗，並對其進行美化與定位排版。
4. **應用邏輯重構 (`app.js`)**：
   - 移除原先在程式碼中寫死的 `squat`, `slr` 等指定動作下拉選單。
   - 將條件檢查與歸位判定（`checkActionCondition`, `checkRestCondition`）重構為以 Custom Action 的 `triggerType`（關節角度、直膝抬高、直膝後擺）進行動態評估。
   - 重構 Joint Visualizer，使其能根據當前動作之 `triggerType` 動態轉換與繪製目標區間弧形。
   - 實作「記憶姿勢」 Modal 內的即時感測器數值監聽（每 100ms 更新）與一鍵 Capture 功能，將其轉換為對應 Trigger Type 的目標角度。
5. **整合測試驗證 (`test_custom_actions.js`)**：
   - 於 Port 3001 啟動測試伺服器，對 CRUD 的 API Endpoint 進行全流程自動化整合測試，所有測試用例均通過且成功銷毀測試數據。
