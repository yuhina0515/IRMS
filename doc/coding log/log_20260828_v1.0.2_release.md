---
tags: [coding-log]
date: 2026-08-28
summary: "v1.0.2 發布:merge feat/no-hardware-exercise-infra 進 main(11 commits,含整個 08-27 無硬體演練基建批次)、bump 版本、npm run dist 打包、gh release create 附安裝檔,涵蓋 v1.0.1 以來累積的所有工作(144→284 tests)。"
---

# 2026-08-28 變更日誌 — v1.0.2 發布

> **相關文件**:[[HOME|導覽首頁]] · [[log_20260714_v1.0.1_release|前次:v1.0.1 發布]] ·
> [[log_20260828_calibration_wearside_and_preview_stutter|同日:校準修復]] ·
> [[log_20260828_firmware_flash_rotation_capture|同日:韌體燒錄]]

## 🎯 目的

使用者在完成校準精靈的兩項修復後,要求「出另外一個版本」。確認範圍是**正式 GitHub
Release**(而非只是本機安裝檔)。

## 🔧 變更內容

1. **發現分支現況**:`git status` 顯示工作目錄在 `feat/no-hardware-exercise-infra`,
   領先本機 `main` 11 個 commit(整個 2026-08-27 批次:ingest 注入接縫、模擬器、
   示範模式、P2 backlog、目視驗收);本機 `main` 又領先 `origin/main` 4 個 commit
   (2026-08-22 的校準凍結快照 + MTU 診斷),自 v1.0.1 發布後這些工作從未推送。
2. 今日未進版控的變更拆成兩個邏輯 commit:
   - `docs: firmware flash + rotation capture, closing issue #2`
   - `fix: calibration wizard preview stutter, add wear-side detection`
3. `git checkout main && git merge --ff-only feat/no-hardware-exercise-infra`——
   乾淨快轉(main 對 feat 沒有任何分歧 commit),58 個檔案變更。
4. `package.json`/`package-lock.json` 版本號 1.0.1→1.0.2。
5. `npm run ci`(typecheck + 284 tests + build)全綠。
6. `npm run dist` 打包,產出 `IRMS Dashboard Setup 1.0.2.exe`(未簽章,signtool
   照例跳過)。
7. 啟動 `release/win-unpacked/IRMS Dashboard.exe` 實機測試:主視窗標題
   「IRMS | 智慧復健監測系統」正確顯示,migration 6/7 於啟動時套用成功
   (`user_version` → 7)。
8. `git push origin main`(4728b45→aae4919)、打 tag `v1.0.2` 並推送、
   `gh release create v1.0.2 --target main` 附上安裝檔與 blockmap。

## 🧠 關鍵取捨

**Release notes 涵蓋範圍**:v1.0.1 到 v1.0.2 之間累積了 07-17 到 08-28 共 40+ 個
commit 的工作,不可能逐條列出(那是 `doc/coding log/` 的職責)。改為挑出對使用者
實際有感的項目分類(Fixed/Added/Changed/硬體驗證狀態/已知限制),並特別點名兩個
**v1.0.1 就在出貨的缺陷**(幻影 reps、invert 翻轉雙倍偏差)——這兩個是使用者已經
在用的版本裡真實存在過的問題,比起淹沒在一長串內部重構清單裡,更值得在 release
notes 開頭獨立點出。

**實機測試沒有用 `--user-data-dir` 隔離**:啟動打包後的 exe 確認它能開機時,忘記
套用專案慣例的隔離啟動,直接用了預設 profile(`C:\Users\Yuhina\AppData\Roaming\
irms-app`)。事後用 Node 內建 `node:sqlite` 唯讀查詢確認:該 DB 的 `sessions`/
`sensor_data` 均為 0 筆,`user_version` 正確落在 7——沒有真實資料存在,migration
本身是既定要套用的變更,沒有造成任何損失,但過程確實偏離了既有的驗證慣例,如實記錄
而非略過不提。

## ✅ 驗證方式

- [x] `npm run ci`(fast-forward 後、bump 版本後各跑一次):typecheck 綠、
      **284 tests**(26 files)全綠、build 成功
- [x] `npm run dist`:產出 `IRMS Dashboard Setup 1.0.2.exe`(~88MB,與
      v1.0.0/v1.0.1 量級一致)
- [x] 打包後 exe 實機啟動:主視窗標題正確、migration 6/7 套用成功
- [x] 啟動所用的預設 profile DB 事後確認 `sessions`/`sensor_data` 均為 0 筆
      (未使用隔離啟動,但確認沒有真實資料受影響)
- [x] `gh release view v1.0.2`:`draft:false`、`prerelease:false`、
      `target:main`、兩個附件(exe + blockmap)皆正確掛上
- [x] `git push origin main` 成功(`aae4919`),tag `v1.0.2` 已推送

## 📝 後續待辦

- 本機 `feat/no-hardware-exercise-infra` 分支已完全併入 `main`(fast-forward,
  無分歧),保留未刪除,之後可視情況清理。
- 下次任何「打包後實機啟動測試」應比照 2026-08-27 的作法,用
  `--user-data-dir` 指向隔離目錄,不要沿用預設 profile——這次只是運氣好
  (該 profile 從未真的存過資料)。
